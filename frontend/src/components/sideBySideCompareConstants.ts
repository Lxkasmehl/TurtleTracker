import classes from './SideBySideCompare.module.css';

/** Two equal columns at every breakpoint (including phones). */
export const SIDE_BY_SIDE_COMPARE_COL_SPAN = 6 as const;

export const sideBySideCompareImageClass = classes.compareImage;
export const sideBySideCompareImageTallClass = classes.compareImageTall;
