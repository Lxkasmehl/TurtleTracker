# Same as requirements.txt but without torch/torchvision — those are installed in
# Dockerfile.cuda from the PyTorch cu128 wheel index (PyPI default wheels are CPU-only).
flask==3.0.0
flask-cors==4.0.0
Pillow>=10.0
# HEIC/HEIF — must stay in sync with requirements.txt (image_utils imports pillow_heif at app startup)
pillow-heif>=0.16.0
numpy>=1.26.0
scikit-learn>=1.3.2
joblib>=1.3.2
werkzeug>=3.0.1
PyJWT>=2.8.0
python-dotenv>=1.0.0
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1

# Test dependencies (integration tests use requests against Docker backend)
pytest>=7.0.0
pytest-flask>=1.3.0
requests>=2.28.0

# Deep Learning / AI (torch + torchvision installed in Dockerfile.cuda from cu121 index)
# Pin LightGlue for reproducible installs
lightglue @ https://github.com/cvg/LightGlue/archive/refs/tags/v0.2.zip
einops
kornia

# Computer Vision (Use headless for Docker/Linux compatibility)
opencv-python-headless>=4.8.0
