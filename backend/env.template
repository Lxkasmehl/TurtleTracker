# Flask Backend Configuration
# Copy this file to .env and update the values
# This file is completely separate from auth-backend/.env

# Server Port (default: 5000)
PORT=5000

# Flask Debug Mode (true/false)
FLASK_DEBUG=true

# JWT Secret - MUST match the JWT_SECRET in auth-backend/.env
# This is used to verify JWT tokens from the auth-backend
# If you change this, make sure to update auth-backend/.env as well
JWT_SECRET=your-secret-key-change-in-production

# Google Sheets API Configuration
# Spreadsheet ID can be found in the Google Sheets URL:
# https://docs.google.com/spreadsheets/d/SPREADSHEET_ID/edit
GOOGLE_SHEETS_SPREADSHEET_ID=your-spreadsheet-id-here

# Path to Google Service Account credentials JSON file
# Download from Google Cloud Console: https://console.cloud.google.com/
GOOGLE_SHEETS_CREDENTIALS_PATH=./credentials/google-sheets-credentials.json

# Community-facing spreadsheet for confirmed turtle data (required for community uploads)
# Confirmed matches and new turtles are always synced here (separate from research spreadsheet).
# Share this spreadsheet with the service account (Editor).
GOOGLE_SHEETS_COMMUNITY_SPREADSHEET_ID=your-community-spreadsheet-id-here
